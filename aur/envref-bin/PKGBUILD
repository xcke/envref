# Maintainer: xcke <xcke@users.noreply.github.com>
# This PKGBUILD is auto-generated by GoReleaser on each release.
# The canonical version lives in the AUR git repo (aur.archlinux.org/envref-bin.git).
# This copy is provided for reference and local testing.

pkgname=envref-bin
pkgver=0.0.0
pkgrel=1
pkgdesc='CLI tool for separating config from secrets in .env files'
arch=('x86_64' 'aarch64')
url='https://github.com/xcke/envref'
license=('MIT')
provides=('envref')
conflicts=('envref')
depends=('glibc')

source_x86_64=("${url}/releases/download/v${pkgver}/envref_${pkgver}_linux_amd64.tar.gz")
source_aarch64=("${url}/releases/download/v${pkgver}/envref_${pkgver}_linux_arm64.tar.gz")

# Checksums are updated by GoReleaser on each release
sha256sums_x86_64=('SKIP')
sha256sums_aarch64=('SKIP')

package() {
    install -Dm755 "./envref" "${pkgdir}/usr/bin/envref"
    install -Dm644 "./LICENSE" "${pkgdir}/usr/share/licenses/envref/LICENSE"
    install -Dm644 "./README.md" "${pkgdir}/usr/share/doc/envref/README.md"
}
